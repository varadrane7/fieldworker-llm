# Fieldworker LLM

Private repository to keep track of any LLM related prompts and outputs.

Make sure to **NEVER** commit `.env` to the github repository.

Keep all prompts in the `prompts/` folder, any python scripts can go in `scripts/` and any documentation can always go in `docs/`
